schema_version: "1.0"
project_id: "codex-orchestrator"
last_updated: "2026-07-29"
project_map_mode: "secondary_memory"

current_priority:
  source: "docs/goals/agent-memory-foundation-v1/state.yaml"
  summary: "Verify the Kit-derived secondary-memory foundation, then capture the evidence-backed control-plane blueprint and launch queues."

canonical_state_pointers:
  run_state: ".orchestrator/runs/<run-id>/run.json"
  goal_state: "docs/goals/<slug>/state.yaml"
  local_queue_state: "queues/*.yaml"

next_recommended_action:
  source: "docs/NEXT_STEPS.md"
  summary: "Run the helper smoke checks and repository tests, then perform the integration phase gate."

session_start_minimum_refs:
  - "AGENTS.md"
  - "docs/NEXT_STEPS.md"
  - "docs/source_of_truth_hierarchy.md"
  - "docs/context_packs/current_status.md"

must_preserve_boundaries:
  - "This file is not task, run, goal, queue, approval, completion, or incident state."
  - "Current owner instructions, code, tests, operational docs, and canonical machine records win."
  - "No Project Map mutation occurs without explicit scope."
